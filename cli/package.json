{
  "name": "mnemo-cli",
  "version": "0.1.0",
  "description": "mnemo CLI - centralized AI memory client",
  "bin": {
    "mnemo": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "typescript": "~5.7.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  }
}
